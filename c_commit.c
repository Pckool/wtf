#include "c_client.h"
#include "h_both.h" 


void commit_c(char* client, char* server){
/*
	DataLink *headcli = (DataLink *) malloc(sizeof(DataLink));
	DataLink *headserv = (DataLink *)malloc(sizeof(DataLink));
        int i = 0;
        int x = 0;
        while(client[i] != '\n'){
                i++;
        }
        while (server[x] != '\n'){
                x++;
        }
        if(x == i){
                int val = strncmp(client, server, i);
                if (val != 0){
                        printf("%s\n", "Version numbers do not not match up.");
                        exit(0);
                }
	}
		tokenizeString(client, 10, headcli);
		tokenizeString(server, 10, headserv);
	while (headserv != NULL){
		int k = strcmp(headserv->token, headcli->token);
			if(k == 0)	
*/
}

